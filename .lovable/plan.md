Remove the resume-onboarding banner from the dashboard.

- In `src/pages/Index.tsx`: remove the `ResumeOnboardingBanner` import and its `<ResumeOnboardingBanner />` usage.
- Delete `src/components/kyc/ResumeOnboardingBanner.tsx` (no other consumers).