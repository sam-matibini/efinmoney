import ClientShell from "@/components/layout/ClientShell";

/** Authenticated pages with header but without KYC gate (profile, security, etc.). */
const ProtectedShell = () => <ClientShell />;

export default ProtectedShell;
