import LiveFxCalculator from "@/components/fx/LiveFxCalculator";

/** Landing-page hero FX widget (dark glass card). */
const FxCalculator = () => <LiveFxCalculator variant="landing" />;

export default FxCalculator;
