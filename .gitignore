# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / secrets
.env
.env.*
!.env.example
scripts/.swychr.env
scripts/.paytota.env

# Exported data snapshot (PII / large)
database-export/

# Migration working files (contain DB credentials / dumps / secrets)
migration-export/

# Supabase CLI local scratch
supabase/.temp/

# Local agent skills tooling (not app code)
.agents/

# Private AI handoff doc (local only — not for git)
AI_HANDOFF.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# TypeScript build info (auto-generated)
*.tsbuildinfo
tsconfig.app.tsbuildinfo

# Office temp lock files
~$*
scripts/.flw-probe.env

scripts/.flw-v4.env
